<!DOCTYPE html>
<html>
 <head>
  <meta charset="UTF-8">
  <title>Basketball Team</title>
 </head>
 <body>
    <h1>Team Name: <%= "#{params[:name]}" %></h1>
    <h4>Coach: <%= "#{params[:coach]}" %></h4>
    <h4>Point Guard: <%= "#{params[:pg]}" %></h4>
    <h4>Shooting Guard: <%= "#{params[:sg]}" %></h4>
    <h4>Small Forward: <%= "#{params[:sf]}" %></h4>
    <h4>Power Forward: <%= "#{params[:pf]}" %></h4>
    <h4>Center: <%= "#{params[:c]}" %></h4>
 </body>
</html>


